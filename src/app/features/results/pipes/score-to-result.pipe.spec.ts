import { ScoreToResultPipe } from './score-to-result.pipe';

describe('ScoreToResultPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreToResultPipe();
    expect(pipe).toBeTruthy();
  });
});
